# AI 微信客服项目统一上下文

本文件是本仓库所有 AI 开发工具和开发者的唯一共享规范来源。工具专用规则只能补充使用方式，不得复制或修改这里的项目事实。

## 开始工作前必须阅读

按以下顺序读取：

1. `AI_CONTEXT.md`
2. `PROJECT_STATUS.md`
3. `docs/tasks/` 中状态为“进行中”的任务
4. 与任务直接相关的架构、协议、数据或部署文档
5. 目标目录内更具体的 `AGENTS.md`（如未来存在）

如果文档与代码不一致，先通过只读检查确认事实，在任务文档记录差异；不得悄悄选择其中一个版本。

## 项目目标

构建可扩展的 AI 微信客服平台，逐步接管设备通信、会话处理、模型路由、长期记忆、知识检索和运营管理。当前仓库处于治理与架构初始化阶段，尚未包含可运行系统。

## 已批准的技术方向

- 采用渐进式重构，不一次性推翻现有 PHP 系统。
- Go 计划负责 TCP 长连接、Protobuf 解码、设备会话和消息路由。
- PHP 现有后台及成熟业务先保留，通过明确接口逐步迁移。
- Python 计划负责模型编排、批处理、记忆抽取和知识图谱集成。
- PostgreSQL 计划作为业务和结构化记忆的主要持久化数据库。
- Redis 计划用于连接状态、缓存、限流和异步队列。
- 所有模型、记忆和知识图谱供应商必须位于项目自有抽象接口之后。
- 腾讯云 Agent Memory 只能通过 `MemoryService` 适配，不得渗透到业务领域模型。
- 知识图谱是第二阶段能力，基础消息存储、检索和记忆评估完成前不引入生产依赖。

这些内容是目标架构，不代表相应代码或基础设施已经存在。

## 不可违反的安全规则

仓库当前公开。任何情况下都不得提交：

- API Key、Access Token、Cookie、私钥、证书私钥、数据库密码或真实账号密码。
- `.env`、本机配置、生产配置或带真实值的配置示例。
- 客户聊天记录、客户资料、通讯录、手机号、微信号、设备唯一标识或认证数据。
- 数据库文件、数据库导出、日志、崩溃转储、抓包或生产消息二进制。
- 未经审计的旧源码压缩包、第三方闭源代码或来源不清的生成文件。
- 名称匹配 `sensitive_*.bin` 的文件。

示例配置只能使用明确的占位值，例如 `<YOUR_API_KEY>`。发现疑似秘密时立即停止提交，把文件移出 Git 跟踪范围，并在任务文档记录处理方式；不得在文档中复制秘密内容。

## 代码与架构约束

- 协议网关、业务服务、AI 服务、记忆服务之间通过版本化接口通信。
- 原始消息必须可追溯，但模型上下文只使用经过授权、裁剪和脱敏的数据。
- 关键写操作必须具备幂等键；消息处理必须允许重试且防止重复副作用。
- 模型调用必须记录供应商、模型、提示词版本、token、耗时、费用估算和结果状态，但不记录明文秘密。
- 模型路由必须支持低成本模型优先、置信度升级、预算上限和人工兜底。
- 供应商不可用时，应降级为队列等待、规则回复或人工接管，不能静默丢消息。
- 未解决的协议假设必须显式记录，不能把推测写成事实。
- 优先编写小而聚焦的模块，避免让单个 AI 一次改动无关子系统。

## 任务文档制度

每次修改都必须对应一个 `docs/tasks/TASK-xxxx.md`。开始任务时写入：

- 目标、背景、范围和不在范围。
- 验收条件、风险、依赖和计划修改的文件。
- 状态：待开始、进行中、受阻或已完成。

工作过程中持续记录关键发现、架构决定、实际修改和验证结果。结束前必须：

1. 更新任务状态和完成项。
2. 写明运行过的验证命令及实际结果。
3. 更新 `PROJECT_STATUS.md`。
4. 对用户可感知或架构性变化更新 `CHANGELOG.md`。
5. 如果形成长期架构决定，在 `docs/decisions/` 新增 ADR。

禁止只在聊天中汇报而不更新仓库文档。

## 修改和验证规则

- 修改前先检查 `git status`，保留用户已有且与任务无关的改动。
- 使用独立分支；不要直接在 `main` 开发。
- 变更范围必须与当前任务一致，不顺手重构无关代码。
- 功能和缺陷修改优先测试先行；文档任务至少运行链接、格式、敏感信息和差异检查。
- 完成声明必须基于本次实际运行的验证输出。
- 未通过验证、未更新任务文档或存在疑似敏感信息时不得提交。

## 多电脑与多 AI 协作

在另一台电脑开始工作：

1. `git clone`（首次）或拉取最新远端状态。
2. 阅读本文件、项目状态和当前任务。
3. 确认没有另一台电脑正在修改同一任务；并行工作使用不同任务号和分支。
4. 开发前同步 `main`，开发后提交分支并创建 Pull Request。
5. 切换电脑前推送当前分支，并把未完成事项写入任务文档。

AI 不得依赖某次聊天的隐藏上下文。任何影响后续工作的事实都必须进入仓库文档。

## 文档导航

- 当前状态：`PROJECT_STATUS.md`
- 变更历史：`CHANGELOG.md`
- 任务记录：`docs/tasks/`
- 架构与模块：`docs/architecture.md`、`docs/module-map.md`
- 通信协议：`docs/protocol.md`
- 数据设计：`docs/database-schema.md`
- 部署容量：`docs/deployment.md`
- 模型路由：`docs/ai-model-routing.md`
- 记忆与知识：`docs/memory-and-knowledge.md`
- 成本估算：`docs/cost-estimate.md`
- 架构决定：`docs/decisions/`
