# Claude Project Entry Point

Read `AI_CONTEXT.md`, then `PROJECT_STATUS.md`, then the active file under `docs/tasks/` before editing. Those files contain the canonical architecture, security, documentation, testing, and multi-computer workflow rules.

Maintain the active task record during work. Before completion, record exact verification results, update project status, and update the changelog when required.

The repository is public. Do not expose credentials, customer/chat data, device identifiers, databases, logs, captures, production payloads, or sensitive binaries.
