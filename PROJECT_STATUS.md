# 项目状态

最后更新：2026-08-20

## 当前阶段

阶段 1A、1B 和 Go 协议网关最小闭环已完成。单设备配对代码和候选镜像已验证，但用户确认实际存在多台已分发且无法升级的旧 APK，因此不再将单设备配对作为正式接入方案。多设备注册表、鉴权、接管、限流、安全配置、管理 CLI 和一次性旧库导入工具已经实现，当前进入容器集成与部署验证。

## 正在进行

- `TASK-0006`：多设备鉴权任务 1—7 已完成；管理命令、只读旧库导入、全批回滚、dry-run 和依赖隔离均已验证，下一步更新容器并执行本地/远程 staging 验证。
- `TASK-0005`：单设备鉴权基础仍作为协议和会话 Token 实现基线；真实设备独占配对不再扩展为正式方案。

## 最近完成

- `TASK-0004`：完成 Go TCP 帧、协议路由、鉴权状态、健康检查、并发关闭修复和 hardened Docker staging；独立复审无 Critical/Important。
- `TASK-0003`：恢复五类最小 `.proto`，提交 15 个公开安全合成帧，并通过 Go/旧 PHP 字段语义、字节往返和未知字段兼容验证。
- `TASK-0002`：完成旧 PHP 源码安全、依赖、许可证、模块和迁移审计；旧源码未导入公开仓库。
- `TASK-0001`：跨 AI 协作规范、架构基线、成本模型和公开仓库安全边界已完成，并通过 [PR #2](https://github.com/1622359590/ai-wechat/pull/2) 合并到 `main`（合并提交 `7ae1cae`）。

## 协作授权

- 在用户本人已认证的 GitHub 环境中，AI 可直接执行分支、提交、推送、PR、验证后合并和合并后同步，不需要逐次请求用户审阅；高风险例外以 `AI_CONTEXT.md` 为准。

## 已确认事实

- 现有系统基于 ThinkPHP、Workerman/Swoole、Redis 队列和 Google Protobuf。
- 当前可复现口径下，排除 `vendor/`、`runtime/` 后有 2,529 个 PHP 文件、392,591 行；微信 Workerman 子树有 138 个 PHP 文件、约 1.44 万行。
- 客服通信采用 TCP 长连接、四字节数据长度和 Protobuf 消息体。
- 原始 `.proto` 尚未找到；已有 439 个生成 PHP 文件（257 个 Jubo 消息类、182 个 `GPBMetadata`）可用于后续恢复。
- 目标 GitHub 仓库 `1622359590/ai-wechat` 当前公开，初始化前为空。

以上数字来自前期本地盘点，后续源码正式导入后必须使用可复现脚本重新统计。

## 已批准方向

- 渐进式重构：Go 网关 + 暂时保留 PHP 业务 + Python AI 服务。
- PostgreSQL + Redis 作为第一阶段数据基础。
- 低成本模型优先，高质量模型按置信度和业务价值升级。
- 腾讯云 Agent Memory 通过自有 `MemoryService` 接口评估接入。
- Graphiti + Neo4j 作为第二阶段知识图谱候选。
- 旧 APK 保持现有 `1010 -> 1011` 协议；多设备使用独立 PostgreSQL 设备库，旧库只作一次性迁移来源，运行时不依赖旧库。

## 下一步候选任务

完整顺序见 `docs/roadmap.md`。当前直接进入：

1. 按 `TASK-0006` 计划实现独立设备库、鉴权、连接接管和限流。
2. 实现受控 CLI 和旧库一次性导入，完成本地 PostgreSQL 与容器集成验证。
3. 先以受控来源验证真实 APK，再决定是否接受旧 APK 的公网明文传输风险。

任务编号只表示候选顺序；开始前应确认优先级和验收范围。

## 当前风险

- 协议文档存在字段不一致、时间单位不一致和安全机制不明确的问题。
- 没有原始 `.proto`，恢复结果需要与真实消息样本和现有 PHP 行为交叉验证。
- 公开仓库不能直接导入未经脱敏和许可证审查的旧源码。
- Agent Memory 当前适合试点验证，生产存储和高可用方案仍需单独评估。
- 远程服务器的宝塔面板目前使用 HTTP，并报告大量待处理系统安全更新；凭据轮换、面板 HTTPS 和系统维护必须在开放业务流量前完成。
- 旧 APK 不受项目方控制，其 Credential 是可冒充的设备编号，且当前无证据表明其支持 TLS；服务端限流、状态检查和短 Token 只能降低风险，无法根治客户端协议缺陷。
- 远程配对入口仍只用于受单一来源限制的 staging；在新设备库和限流实现前不扩大公网来源。
