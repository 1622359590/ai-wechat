# TASK-0004：Go 协议网关最小闭环与测试部署

状态：已完成
负责人：Codex
开始日期：2026-08-19
最后更新：2026-08-19

## 背景

TASK-0003 已提交五类最小 Protobuf 3 协议和 15 个公开安全合成帧，并验证 Go/旧 PHP wire 兼容。当前需要把这些资产接入可运行的 Go TCP 网关，但真实鉴权后端、测试设备和生产 TLS 尚未进入实现范围。

## 目标

- 建立安全默认值的 Go TCP 网关。
- 跑通帧读取、协议解码、鉴权状态、心跳、好友消息和可选发送任务链路。
- 使用已有 15 个 fixture 做回放回归。
- 提供健康检查、优雅关闭和最小容器部署。
- 完成本机 Docker staging；远程目标可用时再部署到测试服务器。

## 不在范围

- 真实客户设备、真实 credential、聊天数据或生产流量。
- PostgreSQL、Redis、PHP HTTP 适配、AI、记忆和知识图谱。
- 公网生产 TLS、域名、负载均衡和高可用。
- 自动接受任意设备的测试后门。

## 验收条件

- [x] Go 根 module 和内嵌最小 schema 可重复构建。
- [x] frame codec 覆盖半包、粘包、零长度、超限和截断。
- [x] protocol codec 回放 15 个 fixture 并严格校验 MsgType/Any 映射。
- [x] 未鉴权连接只能提交设备鉴权；默认鉴权器拒绝全部连接。
- [x] 注入测试鉴权器后可处理心跳、好友消息并编码发送任务。
- [x] TCP server 支持 deadline、连接错误隔离和优雅关闭。
- [x] `/livez`、`/readyz` 行为有自动测试。
- [x] Docker 镜像非 root，Compose 默认只绑定本机回环地址。
- [x] 本机 staging 部署和健康检查通过，或准确记录环境阻断。
- [x] 独立审查无未解决 Critical/Important，公开安全门通过。

## 风险

- 动态描述符适合最小阶段，但高吞吐前需要 benchmark 决定是否生成强类型代码。
- 原协议最大帧长未知；1 MiB 只是安全起点，必须由 TASK-0005 样本调整。
- 默认拒绝鉴权意味着 staging 只验证服务与协议基础，不声称真实设备已接通。
- 当前没有远程服务器 SSH 目标，不能凭空完成远程部署。

## 计划修改文件

- 根 `go.mod`、`go.sum`
- `proto/schema/`
- `internal/frame/`
- `internal/protocol/`
- `internal/gateway/`
- `internal/server/`
- `internal/health/`
- `cmd/gateway/`
- `Dockerfile`、`.dockerignore`、`deploy/compose.yaml`
- `docs/deployment.md`、`PROJECT_STATUS.md`、`CHANGELOG.md`
- 本任务与实施计划

## 实施记录

- 2026-08-19：用户批准继续开发并在完成后部署。
- 2026-08-19：确认 Docker CLI 29.6.2 存在，但 Docker Desktop engine 未响应；SSH config 仅发现 GitHub，没有远程部署主机。
- 2026-08-19：创建 `agent/task-0004-go-gateway`，采用动态描述符和默认拒绝鉴权设计。
- 2026-08-19：先写 schema loader 并发缓存测试；RED 为 `proto/schema` 没有实现文件，补内嵌资源、递归依赖注册和 `sync.Once` 后 GREEN；原兼容测试改为复用同一 loader。
- 2026-08-19：frame 测试先因无实现 RED，随后覆盖半包、粘包、零长度、超限、截断与短写并 GREEN；protocol 测试先因无实现 RED，随后回放 15 个 fixture、严格校验 MsgType/Any、保留未知字段并验证出站消息语义。
- 2026-08-19：handler、TCP server、health 和配置测试均先因实现缺失 RED；补齐默认拒绝鉴权、连接状态、deadline、坏连接隔离、优雅关闭、live/ready 和安全配置后 GREEN；全仓库 race detector 通过。
- 2026-08-19：启动 Docker Desktop；因本机缺少 credential helper 和 BuildKit 插件，使用无认证临时 Docker 配置拉取公开基础镜像，并将 Dockerfile 调整为兼容传统构建器。
- 2026-08-19：构建 3,607,953 字节 scratch 镜像并部署本机 staging；健康状态为 healthy，UID/GID 为 65532:65532，根文件系统只读，19090/18080 均仅绑定 127.0.0.1。
- 2026-08-19：向 TCP 端口发送异常帧后 readiness 仍为 200，容器重启次数为 0；远程部署仍缺 SSH 地址/用户名、登录方式和允许开放端口。
- 2026-08-19：独立审查发现关闭时连接登记竞态和已鉴权连接可重复鉴权两项 Important；分别新增确定性回归测试，确认旧实现 RED 后修复，并以 race `-count=50` 验证 GREEN。
- 2026-08-19：复审无 Critical/Important。对 fallback 健康检查的 Minor 疑问已用实际运行消除：`docker run` 继承镜像 `HEALTHCHECK`，本机 fallback smoke 获得 `healthy` 并通过 ready/live 检查。

## 验证记录

- `go test ./... -race -count=1`：通过。
- `go vet ./...`：通过。
- `LEGACY_SOURCE_ROOT=<legacy-server-root> go test ./proto/compat -count=1`：通过；本机旧源码路径未写入代码或构建产物。
- `sh -n deploy/smoke.sh`：通过。
- `./deploy/smoke.sh`：通过，输出 `staging-smoke=pass`。
- `/livez`、`/readyz`：均返回 200；异常 TCP 帧后仍健康且无重启。
- `go test ./internal/gateway ./internal/server -race -count=50`：通过。
- 独立复审：Critical 0、Important 0，结论 Ready to merge。
